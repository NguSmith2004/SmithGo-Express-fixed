import {useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../lib/api';import AgencyCard from '../components/AgencyCard';import {Search} from 'lucide-react';
export default function Agencies(){const [agencies,setAgencies]=useState([]),[q,setQ]=useState('');const nav=useNavigate();useEffect(()=>{api('/agencies').then(setAgencies)},[]);const filtered=agencies.filter(a=>`${a.name} ${a.address} ${a.description}`.toLowerCase().includes(q.toLowerCase()));return <main className="page"><div className="pagehead"><span className="eyebrow">TRAVEL NETWORK</span><h1>Find your agency</h1><p>Browse the operators available on SmithGo Express and choose the one that fits your trip.</p></div><div className="search"><Search/><input placeholder="Search agencies, locations..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid">{filtered.map(a=><AgencyCard key={a._id} agency={a} onChoose={()=>nav('/book?agency='+a._id)}/>)}</div>{!filtered.length&&<div className="empty">No agencies match your search.</div>}</main>}
