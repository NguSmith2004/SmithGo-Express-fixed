import {useEffect,useState} from 'react';import {useSearchParams,useNavigate} from 'react-router-dom';import {api} from '../lib/api';import {CheckCircle,Download,ArrowLeft} from 'lucide-react';
export default function Confirmation(){const [p]=useSearchParams();const [b,setB]=useState(null);const nav=useNavigate();useEffect(()=>{api('/bookings/mine').then(x=>setB(x.find(i=>i._id===p.get('id')||p.get('bookingId'))))},[]);if(!b)return <main className="page"><div className="empty">Loading confirmation...</div></main>;const download=async()=>{const blob=await api(`/bookings/${b._id}/ticket`);const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${b.reference}.pdf`;a.click();URL.revokeObjectURL(a.href)};return <main className="success"><div className="successcard"><CheckCircle size={62}/><span className="eyebrow">BOOKING CONFIRMED</span><h1>Your trip is reserved.</h1><p>Keep your reference <strong>{b.reference}</strong>. You can download the ticket now or find it later in My Trips.</p><div className="ticketmini"><div><small>AGENCY</small><b>{b.agency.name}</b></div><div><small>ROUTE</small><b>{b.trip.from} → {b.trip.to}</b></div><div><small>DATE & TIME</small><b>{b.trip.departureDate} · {b.trip.departureTime}</b></div><div><small>PASSENGER</small><b>{b.passenger.name}</b></div></div><div className="heroactions"><button className="primary" onClick={download}><Download/> Download ticket</button><button className="secondary" onClick={()=>nav('/bookings')}><ArrowLeft/> My trips</button></div></div></main>}
