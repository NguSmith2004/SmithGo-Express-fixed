import {useEffect,useState} from 'react';import {api} from '../lib/api';import {Download,MapPin,CalendarDays} from 'lucide-react';
export default function Bookings(){const [items,setItems]=useState([]);useEffect(()=>{api('/bookings/mine').then(setItems)},[]);const ticket=async b=>{const blob=await api(`/bookings/${b._id}/ticket`);const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${b.reference}.pdf`;a.click();URL.revokeObjectURL(a.href)};return <main className="page"><div className="pagehead"><span className="eyebrow">YOUR ACCOUNT</span><h1>My trips</h1><p>Your confirmed, completed and cancelled journeys.</p></div><div className="triplist">{items.map(b=><article className="trip" key={b._id}><div className="tripmain"><span className="pill">{b.status}</span><h3>{b.trip.from} <span>→</span> {b.trip.to}</h3><div className="tripmeta"><span><CalendarDays/> {b.trip.departureDate} · {b.trip.departureTime}</span><span><MapPin/> {b.agency.name}</span></div><small>Reference: {b.reference}</small></div><button className="secondary" onClick={()=>ticket(b)}><Download/> Ticket</button></article>)}{!items.length&&<div className="empty">No trips yet. Explore an agency and make your first booking.</div>}</div></main>}
