import {Link,useNavigate} from 'react-router-dom';import {Bus,Menu,X,UserCircle,LogOut,LayoutDashboard} from 'lucide-react';import {useState} from 'react';import {useAuth} from '../context/AuthContext';
export default function Navbar(){const [open,setOpen]=useState(false);const {user,logout}=useAuth();const nav=useNavigate();return <header className="nav"><Link className="brand" to="/"><span className="brandmark"><Bus size={20}/></span><span>SmithGo<span> Express</span></span></Link><button className="menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?'navlinks show':'navlinks'}><Link to="/" onClick={()=>setOpen(false)}>Home</Link><Link to="/agencies" onClick={()=>setOpen(false)}>Agencies</Link>{user&&<Link to="/bookings" onClick={()=>setOpen(false)}>My Trips</Link>}{user?.role==='admin'&&<Link to="/admin" onClick={()=>setOpen(false)}>Admin</Link>}{user?<button className="ghost" onClick={()=>{logout();nav('/')}}><LogOut size={16}/> Logout</button>:<button className="primary small" onClick={()=>nav('/login')}><UserCircle size={17}/> Sign in</button>}</nav></header>}
