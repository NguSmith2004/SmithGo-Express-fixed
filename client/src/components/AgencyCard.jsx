import {motion} from 'framer-motion';import {MapPin,Phone,ArrowUpRight} from 'lucide-react';
export default function AgencyCard({agency,onChoose}){return <motion.article className="agencycard" whileHover={{y:-7}}><div className="agencytop"><div className="logo">{agency.logo?<img src={agency.logo}/>:agency.name.slice(0,2).toUpperCase()}</div><span className="pill">Verified</span></div><h3>{agency.name}</h3><p>{agency.description||'Reliable intercity travel with easy online booking.'}</p><div className="agencyinfo"><span><MapPin size={15}/>{agency.address||'Cameroon'}</span><span><Phone size={15}/>{agency.phone||'Available online'}</span></div><button className="choose" onClick={()=>onChoose?.(agency)}>Choose agency <ArrowUpRight size={17}/></button></motion.article>}
